[build]
  command = "pnpm install --frozen-lockfile"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
