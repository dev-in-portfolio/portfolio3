[build]
  publish = "."
  functions = "netlify/functions"

# Optional: route /api/* to matching function name (strip /api/)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Basic CORS for function responses is handled in code.
